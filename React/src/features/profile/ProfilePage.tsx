import '../panel.css';

export function ProfilePage() {
  return (
    <section className="panel">
      <h2>Profile</h2>
      <p className="lead">
        Skeleton for <code>GET /auth/me</code> and <code>PATCH /auth/me</code> (name, email, password updates).
      </p>
    </section>
  );
}
