import '../panel.css';

export function ListsPage() {
  return (
    <section className="panel">
      <h2>Task lists</h2>
      <p className="lead">
        Skeleton for <code>GET /lists</code>, <code>POST /lists</code>, <code>PATCH /lists/:id</code>,{' '}
        <code>DELETE /lists/:id</code>, and <code>GET /lists/:id/tasks</code>.
      </p>
    </section>
  );
}
